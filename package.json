{
  "name": "quack-monorepo",
  "version": "0.0.1",
  "description": "Quack Monorepo - IMDTravel and AirlinesHub microservices",
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "start": "yarn workspaces foreach run start",
    "start:dev": "yarn workspaces foreach run start:dev",
    "start:prod": "yarn workspaces foreach run start:prod",
    "format": "prettier --write \"apps/**/*.ts\"",
    "lint": "eslint \"apps/**/*.ts\" --fix",
    "test": "yarn workspaces foreach run test",
    "test:watch": "yarn workspaces foreach run test:watch",
    "test:cov": "yarn workspaces foreach run test:cov",
    "test:debug": "yarn workspaces foreach run test:debug",
    "test:e2e": "yarn workspaces foreach run test:e2e"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.7",
    "prettier": "^3.4.2"
  }
}
